server {
	listen 8080;
	listen [::]:8080;
	server_name _;

	root /usr/share/nginx/html;
	index index.html;

	# SPA fallback (SvelteKit adapter-static)
	location / {
		try_files $uri $uri/ /index.html;
	}

	# Do not cache the shell; hashed assets under _app/ may be long-cached by CDN later.
	location = /index.html {
		add_header Cache-Control "no-cache";
	}

	# Security headers (baseline)
	add_header X-Content-Type-Options "nosniff" always;
	add_header Referrer-Policy "strict-origin-when-cross-origin" always;
	add_header X-Frame-Options "DENY" always;
	add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;
	# CSP: static SPA + connect to BFF (browser enforces API origin separately via CORS).
	# Tighten further once font/inline inventory is frozen.
	add_header Content-Security-Policy "default-src 'self'; base-uri 'self'; frame-ancestors 'none'; object-src 'none'; img-src 'self' data:; font-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self'; connect-src 'self' http: https:;" always;

	# Hide nginx version
	server_tokens off;
}
